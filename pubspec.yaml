name: mallard
description: Railway Oriented Programming for Dart. Functional Result and Task types for type-safe error handling.
version: 1.0.0
repository: https://github.com/JakesMD/mallard
issue_tracker: https://github.com/JakesMD/mallard/issues
topics: [mallard, result, task, error-handling, async]

environment:
  sdk: ^3.11.0

workspace:
  - packages/mallard_bloc

dependencies:
  equatable: ^2.0.8

dev_dependencies:
  test: ^1.25.6
  test_beautifier: ^1.1.0
  very_good_analysis: ^10.1.0

screenshots:
  - description: The mallard package image.
    path: mallard.png